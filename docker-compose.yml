services:
    postgres:
        image: postgres:15.3
        environment:
            POSTGRES_USER: "postgres"
            POSTGRES_PASSWORD: "superpassword"
        volumes:
            - ./postgres/initdb:/docker-entrypoint-initdb.d
        ports:
            - "5432:5432"
